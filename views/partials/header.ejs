<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,height=device-height,initial-scale=1.0"/>
    <meta http-equiv="X-UA-Compatible" content="ie=edge">  
    <title>YourCamper</title>
    <link rel="shortcut icon"       href="/favicon.ico?v=2">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="stylesheet" href="/stylesheet/main.css">
</head>
<body>
    <nav class="navbar navbar-default fixed-top">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                  <span class="sr-only">Toggle navigation</span>
                   <i class="fa fa-reorder"></i>
                </button>
    
                <a class="navbar-brand" href="/">YourCamper</a>
            </div>
          <!-- NAVIGATIONS -->  
            <div id="navbar" class="collapse navbar-collapse">
              <!--LEFT NAVS-->
              <ul class="nav navbar-nav">
                <li ><a href="/campgrounds"> <i class="fa fa-home"></i> </a></li>
              </ul>
              <!--RIGHT NAVS-->
              <ul class="nav navbar-nav navbar-right">
                  <% if (!currentUser){ %>
                        <li class="">
                          <a href="/login">
                            Login <i class="glyphicon glyphicon-log-in"></i>
                          </a>
                        </li>
                        <li class=""><a  id="test" href="/register">Sign Up</a></li>
                  <%  } else {  %>
                        <li ><a href="#">Signed In As : <%=currentUser.username%></a></li>
                        <li class="">
                          <a href="/logout">
                            Logout  <i class="glyphicon glyphicon-log-out"></i>
                          </a>
                        </li>
                        
                  <%  } %>
                        <li><a href="/contact-us" target="_blank" rel="noopener noreferrer">Contact Me</a><li>
              </ul>
            </div>
    </nav>
    <!-- Flash MSGS -->
    <div class="container">
        <% if(error && error.length > 0) { %>
        <div class="alert alert-danger" role="alert">
            <%= error %>
        </div>
        <% } %>   
        <% if(success && success.length > 0) { %> 
        <div class="alert alert-success" role="alert">
            <%= success %>
        </div>
        <% } %>
    </div>
    